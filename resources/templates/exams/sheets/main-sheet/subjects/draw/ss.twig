{% import "exams/general/absence.twig" as absence %}

{% if entity.draw.ss.evaluation.isAbsence %}
	{{ absence.absenceCell(entity.draw.ss.evaluation.value) }}
{% else %}
	<td>{{ entity.draw.ss.evaluation.value }}</td>
{% endif %}

{% if entity.draw.ss.written.isAbsence %}
	{{ absence.absenceCell(entity.draw.ss.written.value) }}
{% else %}
	<td>{{entity.draw.ss.written.value}}</td>
{% endif %}
