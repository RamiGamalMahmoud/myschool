{% import "exams/general/absence.twig" as absence %}

{% if entity.socials.ss.evaluation.isAbsence %}
	{{ absence.absenceCell(entity.socials.ss.evaluation.value) }}
{% else %}
	<td>{{ entity.socials.ss.evaluation.value }}</td>
{% endif %}

{% if entity.socials.ss.written.isAbsence %}
	{{ absence.absenceCell(entity.socials.ss.written.value) }}
{% else %}
	<td>{{entity.socials.ss.written.value}}</td>
{% endif %}
